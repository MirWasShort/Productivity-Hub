{
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }],
  // Non usato per compilare (files: []), ma è qui che la CLI di shadcn/ui
  // cerca l'alias per capire dove scrivere i componenti.
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
